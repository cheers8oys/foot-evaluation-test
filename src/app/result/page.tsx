import { AppShell } from "@/components/app-shell";

export default function ResultPage() {
  return (
    <AppShell title="결과지" description="결과 URL 검증과 콘텐츠 매핑은 후속 단계에서 구현합니다.">
      <div className="placeholder-card">
        <p>대표 결과, 추가 패턴, 운동 영상, 추천 제품 영역은 아직 비어 있습니다.</p>
      </div>
    </AppShell>
  );
}
