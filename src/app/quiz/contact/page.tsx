import { AppShell } from "@/components/app-shell";

export default function QuizContactPage() {
  return (
    <AppShell
      title="연락처 입력"
      description="이름, 전화번호, 동의 입력 폼은 후속 단계에서 구현합니다."
    >
      <div className="placeholder-card">
        <p>폼 UI, 유효성 검사, 제출 API 연결은 아직 비어 있습니다.</p>
      </div>
    </AppShell>
  );
}
