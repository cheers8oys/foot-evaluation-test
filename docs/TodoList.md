# TodoList

> 공통 작업 원칙은 `AGENTS.md`, 상세 엔지니어링 규칙은 `docs/engineering-guardrails.md`를 우선한다.
>
> 제품 요구사항은 `docs/siztank-foot-type-test-prd-v1.2-implementation.md`, 화면 요구사항은 `docs/siztank-foot-type-test-screen-spec-v1.0.md`, 구현 구조는 `docs/plan.md`, 메시지 연동은 `docs/munjaon-alimtalk-integration-guide.md`를 참조한다.

## 0. 개발 착수 준비

- [ ] 현재 작업트리 상태 확인
- [ ] 의존성 설치 상태 확인
- [ ] 환경변수 목록과 외부 연동 준비 범위 확인
- [ ] 문서 기준 최신 스펙 확인

검증:

- `git status --short`
- `npm install` 완료 여부 확인
- `docs/plan.md`, PRD, 화면설계서, 문자온 가이드 최신 내용 확인

완료 기준:

- 로컬 실행과 검증 명령을 돌릴 수 있다.
- 구현 범위와 제외 범위를 명확히 설명할 수 있다.

## 1. 테스트 기반 개발 환경 고정

- [ ] 테스트 파일 위치와 네이밍 규칙 확정
- [ ] 도메인 로직 테스트 우선순위 정리
- [ ] UI 테스트와 서버 로직 테스트 기준 분리
- [ ] 검증 루틴을 작업 습관에 맞게 고정

검증:

- `npm run format`
- `npm run lint`
- `npm run typecheck`
- `npm run test:run`

완료 기준:

- 새 기능 작업 시 어디에 어떤 테스트를 먼저 추가할지 즉시 판단할 수 있다.

## 2. 앱 기본 골격 구성

- [ ] `src/app` 라우트 기본 구조 생성
- [ ] 공통 레이아웃, 기본 메타데이터, 글로벌 스타일 정리
- [ ] 모바일 우선 폭과 공통 여백 정책 반영
- [ ] `not-found`, `privacy`, `result`, `quiz/*` 라우트 자리 마련

검증:

- `npm run build`
- 홈(`/`)과 기본 라우트가 빌드에서 인식되는지 확인

완료 기준:

- 제품 기능 없이도 페이지 구조가 정상 빌드된다.

## 3. 상수와 타입 정의

- [ ] 질문 데이터 상수 작성
- [ ] 결과 케이스 상수 작성
- [ ] 이름, 전화번호, 답변, 결과 타입 정의
- [ ] 개인정보/동의 문구 상수 분리

검증:

- 타입 파일에 대한 단위 테스트 또는 import smoke test 작성
- `npm run typecheck`

완료 기준:

- 질문, 결과, 문구가 하드코딩 없이 상수/타입으로 일관되게 참조된다.

## 4. 핵심 도메인 로직 구현

- [ ] `diagnose()` 테스트 먼저 작성
- [ ] 우선순위 포함 결과 계산 로직 구현
- [ ] 결과 URL 생성/검증 로직 테스트 먼저 작성
- [ ] 결과 URL 유틸 구현
- [ ] 이름 trim/길이 검증 유틸 테스트 먼저 작성
- [ ] 전화번호 정규화/검증 유틸 테스트 먼저 작성

검증:

- `diagnose()` 정상 케이스
- 복수 케이스 매칭 시 우선순위 케이스 선택
- `default` 단독 처리
- 잘못된 결과 URL 파라미터 거부
- 이름 공백 trim, 빈 문자열 거부, 30자 초과 거부
- 휴대폰 번호 형식 검증
- `npm run test:run`

완료 기준:

- 결과 계산, URL 검증, 입력 검증이 테스트로 고정된다.

## 5. sessionStorage 상태 관리 구현

- [ ] 시작 시각, 토큰, 답변, 결과 저장 유틸 작성
- [ ] 만료 체크 로직 테스트 먼저 작성
- [ ] 새로고침 복원 로직 테스트 먼저 작성
- [ ] 초기화 유틸 작성

검증:

- 30분 이내 상태 복원
- 30분 초과 시 초기화
- 누락 답변이 있으면 가장 이른 미응답 단계로 유도
- `npm run test:run`

완료 기준:

- 클라이언트 진행 상태가 문서 요구사항대로 복원/만료 처리된다.

## 6. 인트로와 문항 화면 구현

- [ ] `/` 인트로 화면 구현
- [ ] `/quiz/[step]` 진행 표시와 A/B 선택 UI 구현
- [ ] 이전/다음 이동 구현
- [ ] 선택 즉시 답변 저장 연결
- [ ] 8번 완료 시 결과 계산 후 analyzing 이동 연결

검증:

- 인트로에서 시작 버튼 동작
- step 범위 외 접근 시 404 또는 차단
- 답변 미선택 시 다음 버튼 비활성
- 이전/다음 이동 시 선택 상태 유지
- `npm run test:run`
- `npm run build`

완료 기준:

- 1~8번 문항 플로우가 UI와 상태 저장까지 일관되게 동작한다.

## 7. 분석 중 화면과 연락처 입력 화면 구현

- [ ] `/quiz/analyzing` 자동 이동 구현
- [ ] `/quiz/contact` 이름 입력창 구현
- [ ] `/quiz/contact` 전화번호 입력창 구현
- [ ] 동의 체크박스와 개인정보 문구 구현
- [ ] CTA 활성 조건 구현
- [ ] 클라이언트 유효성 검사 구현

검증:

- 이름 미입력 시 CTA 비활성 또는 오류
- 전화번호 형식 오류 처리
- 체크박스 미선택 시 제출 불가
- analyzing 진입 시 결과 없으면 재계산 또는 홈 이동
- `npm run test:run`

완료 기준:

- 이름, 전화번호, 동의가 요구사항대로 수집된다.

## 8. 결과 페이지와 제출 완료 화면 구현

- [ ] `/quiz/submitted` 상태별 UI 구현
- [ ] `/result` 대표 결과/추가 패턴 UI 구현
- [ ] `primary`, `cases` URL 검증 연결
- [ ] `/privacy`와 `404` 화면 구현

검증:

- `created`/`duplicate` 상태별 화면 분기
- 잘못된 결과 URL 접근 차단
- `default` 단독 처리
- 개인정보 처리방침 접근 가능
- `npm run test:run`
- `npm run build`

완료 기준:

- 제출 후 사용자 이동 경로와 결과 확인 흐름이 완성된다.

## 9. 서버 세션 시작 API 구현

- [ ] `POST /api/session/start` request/response 테스트 먼저 작성
- [ ] 서명 토큰 발급 구현
- [ ] 30분 만료 포함 구현
- [ ] 환경변수 검증 연결

검증:

- 정상 토큰 발급
- 응답 형식 확인
- 비밀키 누락 시 명확한 실패
- `npm run test:run`

완료 기준:

- 클라이언트가 시작 토큰을 안정적으로 발급받을 수 있다.

## 10. 서버 제출 API 기본 검증 구현

- [ ] `POST /api/submit` request schema 테스트 먼저 작성
- [ ] 이름 검증 구현
- [ ] 전화번호 검증 구현
- [ ] 동의 체크 검증 구현
- [ ] 답변/결과 재계산 검증 구현
- [ ] 토큰 검증 구현
- [ ] 15초 미만 제출 차단 구현

검증:

- `INVALID_NAME`
- `INVALID_PHONE`
- `CONSENT_REQUIRED`
- `INVALID_ANSWERS`
- `TOKEN_REQUIRED`
- `INVALID_TOKEN`
- `TOKEN_EXPIRED`
- `TOO_FAST`
- `npm run test:run`

완료 기준:

- 외부 연동 없이도 제출 API의 핵심 검증 로직이 안정적이다.

## 11. Google Sheets 연동 구현

- [ ] 시트 클라이언트 래퍼 작성
- [ ] 이름/전화번호/결과/동의 버전 row 추가 구현
- [ ] 전화번호 기준 중복 검색 구현
- [ ] 쓰기 직전 재확인 구현
- [ ] 실패 에러 매핑 구현

검증:

- 신규 번호 저장
- 기존 번호 duplicate 응답
- 이름과 전화번호가 올바른 컬럼에 저장
- 저장 실패 시 `SHEET_WRITE_FAILED`
- `npm run test:run`

완료 기준:

- 신규 사용자만 시트에 1회 저장되고, 중복 번호는 저장되지 않는다.

## 12. 문자온 발송 연동 구현

- [ ] 메시지 본문 builder 구현
- [ ] fallback SMS builder 구현
- [ ] 문자온 client 구현
- [ ] `MESSAGE_PROVIDER` 분기 구현
- [ ] 성공/실패 응답 매핑 구현

검증:

- 알림톡 성공 응답 처리
- 문자온 실패 시 `MESSAGE_SEND_FAILED`
- 기존 번호에는 발송 호출 없음
- 테스트 모드 `MUNJAON_TEST_YN=YS` 흐름 확인
- `npm run test:run`

완료 기준:

- 신규 저장 성공 후 결과 링크 발송까지 서버 흐름이 닫힌다.

## 13. 제출 플로우 통합

- [ ] `/quiz/contact`와 `/api/submit` 연결
- [ ] `created`/`duplicate` 라우팅 연결
- [ ] 서버 메시지와 클라이언트 오류 표시 연결
- [ ] 세션 초기화와 재시작 흐름 연결

검증:

- 정상 신규 사용자 end-to-end 플로우
- 기존 번호 duplicate 플로우
- 토큰 만료 후 재시작 플로우
- `npm run test:run`
- `npm run build`

완료 기준:

- 사용자 플로우 전체가 실제 제출 시나리오 기준으로 동작한다.

## 14. 분석 이벤트 연결

- [ ] 이벤트 호출 유틸 작성
- [ ] `test_start`, `quiz_answer`, `quiz_complete`, `contact_view`, `submit_success`, `submit_duplicate`, `submit_error`, `result_view` 연결
- [ ] 개인정보가 이벤트 payload에 포함되지 않도록 확인

검증:

- 이벤트 호출 시 이름/전화번호 미포함
- 주요 전환 시점에 이벤트 누락 없음
- `npm run test:run`

완료 기준:

- 핵심 전환 이벤트가 개인정보 없이 수집된다.

## 15. 반응형 UI와 문구 최종 점검

- [ ] 360px, 390px, 430px, 데스크톱 480px 중앙 레이아웃 점검
- [ ] 버튼, 카드, 입력창 줄바꿈/겹침 점검
- [ ] placeholder와 준비 중 문구 점검
- [ ] 의료 진단처럼 보이는 표현 제거 점검

검증:

- 모바일 폭에서 UI 깨짐 없음
- CTA와 폼 요소가 터치 가능 크기 유지
- 텍스트 겹침 없음
- 수동 확인 결과 기록

완료 기준:

- 화면설계서의 모바일 우선 레이아웃 요구를 충족한다.

## 16. 보안과 개인정보 최종 점검

- [ ] 실데이터/실자격증명 미포함 확인
- [ ] 환경변수 명칭과 사용 위치 점검
- [ ] 서버 로그에 개인정보 원문이 남지 않는지 점검
- [ ] 개인정보 처리방침 문구와 동의 문구 상수화 확인

검증:

- `git diff --cached`
- `git status --short`
- `rg`로 이름, 전화번호, key, secret, private key 패턴 점검
- `npm run verify`

완료 기준:

- 커밋 가능한 상태이며 개인정보/비밀정보 누출 위험이 없다.

## 17. 최종 통합 QA

- [ ] 신규 사용자 플로우
- [ ] 기존 번호 플로우
- [ ] 이름 오류 플로우
- [ ] 전화번호 오류 플로우
- [ ] 미동의 플로우
- [ ] 토큰 오류/만료 플로우
- [ ] 결과 URL 변조 플로우
- [ ] 발송 실패 플로우

검증:

- `npm run verify`
- 체크리스트별 수동 QA 결과 기록

완료 기준:

- PRD 구현 완료 기준과 화면설계서 QA 항목을 모두 통과한다.

## 18. 커밋 및 배포 준비

- [ ] 변경 파일 의도 확인
- [ ] 단계별 또는 변곡점별 커밋 분리
- [ ] push 전 최종 verify
- [ ] 배포 전 확정 필요 항목 재확인

검증:

- `git diff --cached --stat`
- `git diff --cached`
- `npm run verify`

완료 기준:

- 커밋 메시지가 변곡점 단위로 분리되어 있고, push 가능한 상태다.
